extends: [configs/base.yaml, configs/server.yaml]
experiment:
  name: no_retrieval
  split: test
models:
  generation_model: mistral-large:123b
prompting:
  type: structured
  max_tokens: 384
retrieval:
  enabled: false
  use_reranker: false
backends:
  generation:
    timeout_seconds: 600
