extends: [configs/base.yaml, configs/server.yaml]
experiment:
  name: model_comparison
  split: test
prompting:
  type: retrieval_augmented
backends:
  generation:
    timeout_seconds: 600
ablations:
  models: [mistral-large:123b, llama3.3:70b-instruct-q8_0]
