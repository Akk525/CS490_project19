extends: [configs/base.yaml, configs/server.yaml]
experiment:
  name: llama_main
  split: test
models:
  generation_model: llama3.3:70b-instruct-q8_0
prompting:
  type: retrieval_augmented
backends:
  generation:
    timeout_seconds: 600
