extends: [configs/base.yaml, configs/server.yaml]
experiment:
  name: retrieval_strategy
  split: test
models:
  generation_model: mistral-large:123b
prompting:
  type: retrieval_augmented
backends:
  generation:
    timeout_seconds: 600
ablations:
  retrieval_strategies: [disruption_only, full_context, hybrid]
